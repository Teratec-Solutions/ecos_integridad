import { AuthContext, AuthProvider, useAuthContext } from './Auth.context'
import { ClientsContext, ClientsProvider, useClientsContext } from './Clients.context'
import { UsersContext, UsersProvider, useUsersContext } from './Users.context'

export default {
    AuthContext,
    AuthProvider,
    useAuthContext,
    UsersContext,
    UsersProvider,
    useUsersContext,
    ClientsContext,
    ClientsProvider,
    useClientsContext
}