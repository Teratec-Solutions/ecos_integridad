import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ecos.integridad.ts',
  appName: 'ecos_integridad',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
